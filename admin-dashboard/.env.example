# Where the Coordinator REST API lives (server side only, not exposed to the browser).
COORDINATOR_API_URL=http://localhost:8081

# Beta admin credentials.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Secret used to sign the admin session cookie. Set a long random value in production.
AUTH_SECRET=change-me-to-a-long-random-string
